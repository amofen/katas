package main

import (
	"errors"
)

func main() {
	/**
		* requirements : to not use libraries (implement algorithmes from scratch)
		* command : base64 ->  no flag : encode , -d flag : decode
		* command : crypt -> -e : encrypt , -d : decrypt, -k key , -c content to encrypt
	**/
}

func Base64Encode(content string) (string, error) {
	//Do thebase64 encoding operations here .
	encodedContent := ""
	if false {
		return "", errors.New("Huh , this is not working !!!")
	} else {
		return encodedContent, nil
	}
}

func Base64Decode(encodedContent string) (string, error) {
	content := ""
	if false {
		return "", errors.New("Error, don't try again with the same parameters, it won't work bro!")
	} else {
		return content, nil
	}
}
